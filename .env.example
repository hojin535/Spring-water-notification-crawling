# =================================================
# MYSQL 데이터베이스 설정
# =================================================
# 로컬 MySQL 연결 (개발 환경)
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=your-mysql-password
MYSQL_DATABASE=Spring_water_notification

# =================================================
# SSH 터널링 설정 (선택사항)
# =================================================
# 원격 서버의 MySQL에 SSH 터널을 통해 연결하는 경우
# USE_SSH_TUNNEL=true
# SSH_HOST=your-server.com
# SSH_PORT=22
# SSH_USER=your-username
# SSH_PASSWORD=your-ssh-password
# 또는 SSH 키 사용 (권장):
# SSH_KEY_PATH=/Users/yourname/.ssh/id_rsa

# =================================================
# SMTP 이메일 설정
# =================================================
# Gmail을 사용하는 경우:
# 1. Google 계정 > 보안 > 2단계 인증 활성화
# 2. 앱 비밀번호 생성: https://myaccount.google.com/apppasswords
# 3. 생성된 16자리 비밀번호를 SMTP_PASSWORD에 입력

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM_EMAIL=your-email@gmail.com
SMTP_FROM_NAME=Spring Water Notification

# 이메일 인증 링크용 베이스 URL
BASE_URL=http://localhost:8000

# 알림 기능 활성화 여부
NOTIFICATION_ENABLED=true

# =================================================
# 기존 설정 (유지)
# =================================================
# 서버 시작 시 크롤링 실행 여부
CRAWL_ON_STARTUP=false

# Google Gemini API 키 (AI 설명 기능용)
# GEMINI_API_KEY=your-api-key-here
